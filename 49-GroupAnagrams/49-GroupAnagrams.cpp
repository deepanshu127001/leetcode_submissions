        for(string str:strs)
        {
            auto key = str;
        }
            sort(key.begin(),key.end());
            mp[key].push_back(str);
        while(iter!=mp.end()){
        auto iter = mp.begin();
            vec.push_back(iter->second);
        }
            iter++;
        return vec;
    }
};
        vector<vector<string>> vec;
[
