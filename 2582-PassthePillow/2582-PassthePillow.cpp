                currentPosition++;
            }

            if(currentPosition == n)
            else{
            }
                currentPosition--;
                isReverse = true;
            else if(currentPosition ==1)
                isReverse = false;
        }
return currentPosition;
    }
};
