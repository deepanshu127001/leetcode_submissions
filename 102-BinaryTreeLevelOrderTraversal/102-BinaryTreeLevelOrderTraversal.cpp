            int queue_size = q.size();
            if(queue_size==0)return ans;
            while(queue_size > 0){
                TreeNode *node = q.front();
                q.pop();
                nodes.push_back(node->val);
                if(node->left)
            vector<int> nodes;
        while(1){
            return ans;
        if(root==nullptr)
        q.push(root);
        queue<TreeNode*> q;
        vector<vector<int>> ans;
                    q.push(node->left);
                if(node->right)
                    q.push(node->right);
            queue_size--;
            }
[
